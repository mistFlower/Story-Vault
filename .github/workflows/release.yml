name: Release

# main 에 앱 소스가 바뀌면 빌드해서 릴리스한다.
# 원고(vault/)와 문서만 바뀐 커밋은 앱과 무관하므로 빌드하지 않는다.
on:
  push:
    branches: [main]
    paths-ignore:
      - 'vault/**'
      - 'docs/**'
      - '**.md'
      - '.gitignore'
  workflow_dispatch:

# 릴리스가 겹치지 않게 한 번에 하나만 돈다.
concurrency:
  group: release
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: npm

      - run: npm ci
      - run: npx tsc --noEmit
      - run: npx vitest run

  release:
    needs: test
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: npm

      - uses: dtolnay/rust-toolchain@stable

      - uses: swatinem/rust-cache@v2
        with:
          workspaces: './src-tauri -> target'

      - run: npm ci

      # Rust 테스트는 여기서 돈다. 개발 PC 의 mingw 툴체인에서는 테스트
      # 바이너리가 STATUS_ENTRYPOINT_NOT_FOUND 로 죽어 로컬 실행이
      # 불가능하다. CI 는 MSVC 라 정상 동작한다.
      # 빌드보다 먼저 돌려 실패 시 릴리스가 나가지 않게 한다.
      - name: Rust 테스트
        run: cargo test --manifest-path src-tauri/Cargo.toml --lib

      # 커밋마다 버전을 올려야 업데이터가 새 버전으로 인식한다.
      # run_number 는 단조 증가하므로 패치 번호로 그대로 쓴다.
      - name: 버전 결정
        id: version
        shell: bash
        run: |
          BASE=$(node -p "require('./package.json').version.split('.').slice(0,2).join('.')")
          VERSION="${BASE}.${{ github.run_number }}"
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
          node -e "
            const fs = require('fs');
            for (const f of ['package.json', 'src-tauri/tauri.conf.json']) {
              const j = JSON.parse(fs.readFileSync(f, 'utf8'));
              j.version = '$VERSION';
              fs.writeFileSync(f, JSON.stringify(j, null, 2) + '\n');
            }
            const toml = fs.readFileSync('src-tauri/Cargo.toml', 'utf8')
              .replace(/^version = \".*\"/m, 'version = \"$VERSION\"');
            fs.writeFileSync('src-tauri/Cargo.toml', toml);
          "
          echo "빌드 버전: $VERSION"

      - name: 빌드 및 릴리스
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
        with:
          tagName: v${{ steps.version.outputs.version }}
          releaseName: Story Vault v${{ steps.version.outputs.version }}
          releaseBody: |
            ${{ github.event.head_commit.message }}

            ---
            실행 중인 Story Vault 는 다음 실행 시 이 버전으로 자동 업데이트됩니다.
          releaseDraft: false
          prerelease: false
          # latest.json 을 릴리스에 첨부해야 업데이터가 새 버전을 찾을 수 있다.
          includeUpdaterJson: true
